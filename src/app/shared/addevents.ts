export interface Addevents {
    event_name:string,
    discription:string,
    from_date : string,
    to_date  : string,
    event_organizer : string,
    institution_id : number,
    academic_id : number
 
}
